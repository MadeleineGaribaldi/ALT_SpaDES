After knitting and rendering `ALThickness.Rmd`, overwrite this file with a symlink to `ALThickness.md`.

E.g., on Linux/macOS from terminal:

```bash
cd C:/Users/mgaribal/Documents/test/modules/ALThickness
rm README.md && ln -s ALThickness.md README.md
```
