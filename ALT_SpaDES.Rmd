---
title: "ALT_SpaDES Manual"
subtitle: "v.`r SpaDES.core::moduleMetadata(module = 'ALT_SpaDES', path = '..')$version`"
date: "Last updated: `r Sys.Date()`"
output:
  bookdown::html_document2:
    toc: true
    toc_float: true
    theme: sandstone
    number_sections: false
    df_print: paged
    keep_md: yes
editor_options:
  chunk_output_type: console
bibliography: citations/references_ALT_SpaDES.bib
link-citations: true
always_allow_html: true
---

# ALT_SpaDES Module

<!-- the following are text references used in captions for LaTeX compatibility -->

(ref:ALT_SpaDES) *ALT_SpaDES* #MG do include the other parameters here?

```{r setup-ALT_SpaDES, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, eval = FALSE, results = "hold") ## change to eval = TRUE if all chunks are to be evaluated

dir.create("figures", showWarnings = FALSE)

download.file(url = "https://img.shields.io/badge/Made%20with-Markdown-1f425f.png",
              destfile = "figures/markdownBadge.png",
              mode = "wb")
```

[![made-with-Markdown](figures/markdownBadge.png)](https://commonmark.org)

<!-- if knitting to pdf remember to add the pandoc_args: ["--extract-media", "."] option to yml in order to get the badge images -->

#### Authors:

`r paste(as.character(SpaDES.core::moduleMetadata(module = "ALT_SpaDES", path = '..')$authors), sep = ', ')` <!-- ideally separate authors with new lines, '\n' not working -->

## Module Overview

Estimates active layer thickness

### Quick links

-   [General functioning](#general-functioning)

-   [List of input objects](#input-list)

-   [List of parameters](#params-list)

-   [List of outputs](#outputs-list)

-   [Simulation flow and module events](#sim-flow)

### Module summary

ALT_SpaDES module calculates active layer thickness using annual ground surface temperature, surface temperature amplitude, and 4 parameters calibrated based on land cover type. As the equation uses exponentials the annual ground surface temperature must be provided in KELVIN. The module runs the ALT Solver function monthly from May to October and summarizes the greatest depth where the ground remains greater than 0 during this period (i.e. the active layer thickness). 

### Module inputs and parameters

Parameters
grid_ppp -> sets the number of grid points per period. This determines how often the function is sampled within each sine wave. Increasing this value will ensure all crossings of f(z) = 0 but can increase processing time. If this value is decreased processing time will increase but crossings may be missed.

overresolve -> multiplier than increases the number of grid points beyond the minimum assigned by grid_ppp. 

plot_check -> default is FALSE. If set to TRUE module will create a diagnostic plot for each month (5-10) showing the behavior of the ALT Solver Function and can indicate if there is an active layer present and the depth. This can be used for validation and debugging, however it greatly increases the processing time.

tol -> tolerance which sets the numerical accuracy (number of decimal points) reported for the active layer thickness

verbose -> default is FALSE. If set to TRUE the module will give diagnostic messages while the solver runs. This can be useful for validation and debugging, but can increase processing time. 

z_search -> range of depths (z) for the ALT Solver. Default is 0-5m. The module will produce results up to the maximum provided. This can lead to erroneous prediction of active layer thickness where no permafrost is present. The ALT Solver has only been validated up to 3m, therefore running the module for depths much greater than 3m may produce inaccurate results. It is recommended to limit the model run to within typical active layer thicknesses (< 5m).

Describe input data required by the module and how to obtain it (e.g., directly from online sources or supplied by other modules) If `sourceURL` is specified, `downloadData("ALT_SpaDES", "C:/Users/mgaribal/Documents/test/modules")` may be sufficient.

Inputs 

siteInfo -> Data table input with the site information. The columns of this data table are Site (the site names), Landcover, Year, Ts (the annual ground surface temperature in Kelvin), and A (the ground surface temperature amplitude, the difference between the monthly maximum and minimum ground surface temperature). Sample data is provided to show module function but should be replaces with real data before use.

gParameters -> Data table input with the parameters required in the ALT Solver. The columns of this data table are Landcover, d (damping depth), k (decay in annual temperature), b (the amplitude modifier), p (the period modifier). This input can be created using the GT_Calibration_SpaDES module if the parameters need to be calibrated for new environment and/or land cover types. Currently the parameters provided in this data table have been calibrated for peatland land covers. 

Table \@ref(tab:moduleInputs-ALT_SpaDES) shows the full list of module inputs.

```{r moduleInputs-ALT_SpaDES, echo = FALSE, eval = TRUE, message = FALSE, warning = FALSE}
#Do I need to change the file path?
df_inputs <- SpaDES.core::moduleInputs("ALT_SpaDES", "C:/Users/mgaribal/Documents/test/modules")
knitr::kable(df_inputs,
             caption = "List of (ref:ALT_SpaDES) input objects and their description.") |>
  kableExtra::kable_styling(latex_options = "scale_down", full_width = TRUE)
```

Summary of user-visible parameters (Table \@ref(tab:moduleParams-ALT_SpaDES))

```{r moduleParams-ALT_SpaDES, echo = FALSE, eval = TRUE, message = FALSE, warning = FALSE}
#change file path?
df_params <- SpaDES.core::moduleParams("ALT_SpaDES", "C:/Users/mgaribal/Documents/test/modules")
knitr::kable(df_params, caption = "List of (ref:ALT_SpaDES) parameters and their description.") |>
  kableExtra::kable_styling(latex_options = "scale_down", full_width = TRUE)
```

### Events

Init -> This event sets up a data table to avoid a loop within the ALT Solver. First the two input data tables siteInfo and gParameters are combined based on land cover type into a siteParameters data table. It then creates a new data table of months (5-10). The new months data table and the siteParameters data table are combined to duplicate the rows of siteParameters five times (one for each month in the months data table) and to create a new column months. This is done to avoid a loop within the ALT Solver. These changes are saved in a new data table called ALTparameters.

ALTcalc -> This event runs the ALT Solver function for each row in the ALTparameters data table and saves each root (crossing of f(z) = 0)

ALTfinal -> This event calculate the final active layer thickness for each site and year provided in the siteInfo data table. The active layer thickness is the maximum root depth (greatest z where f(z) crosses 0) for each individual year and site

### Plotting

Write what is plotted.

### Module outputs

ALTfinal -> Data table containing the final active layer thickness (in meters) for each site and year in the siteInfo data table input. 

(Table \@ref(tab:moduleOutputs-ALT_SpaDES)).

```{r moduleOutputs-ALT_SpaDES, echo = FALSE, eval = TRUE, message = FALSE, warning = FALSE}
df_outputs <- SpaDES.core::moduleOutputs("ALT_SpaDES", "C:/Users/mgaribal/Documents/test/modules")
knitr::kable(df_outputs, caption = "List of (ref:ALT_SpaDES) outputs and their description.") |>
  kableExtra::kable_styling(latex_options = "scale_down", full_width = TRUE)
```

### Links to other modules

The gParameters data table can be produced from the GT_Calibration_SpaDES module if parameters need to be calibrated for a new enivornment or landcover types. 

The siteInfo data table can be produced from the ###### module if the users does not have measured ground surface temperature and amplitude. 

### Getting help

-   provide a way for people to obtain help (e.g., module repository issues page)

## References

<!-- autogenerated from bibligraphy -->
